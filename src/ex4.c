/**
 * This ex4 should be with Valgrind 
 * But it cannot run in ubuntu, cannot run make, cannot install
 * It just broken.
 * So it's not possible to use in the later ex.
 **/

#include <stdio.h>

int main()
{
    int age = 10;
    int height;

    printf("I am %d years old.\n");
    printf("I am %d inches tall.\n", height);

    return 0;
}